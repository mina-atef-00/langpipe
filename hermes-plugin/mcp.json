{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "pipe": {
      "type": "stdio",
      "command": "python3",
      "args": [
        "${PLUGIN_ROOT}/server/lesan_pipe_mcp.py"
      ],
      "cwd": "${PLUGIN_DATA}"
    }
  }
}
